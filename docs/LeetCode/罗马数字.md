
<img src="./pictures/Annotation 2020-04-05 190456.png
"  div align=center />
贪心算法
```java
class Solution {
    public String intToRoman(int num) {
        StringBuilder sb = new StringBuilder("");
        int[] integers = {1000, 900, 500, 400,100,90 ,50,40,10,9, 5,4,1};
        String[] strings = {"M", "CM", "D","CD","C","XC", "L","XL","X","IX","V","IV","I"};
        int remain = num;
        int index = 0;
        while(remain>0&&index<=12){
            if(remain>=integers[index]){
                remain -= integers[index];
                sb.append(strings[index]);
                if(remain<integers[index]){
                    index++;
                }
            }else{
                index++;
            }
        }
        return sb.toString();
    }
}

```