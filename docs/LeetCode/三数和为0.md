<img src="./pictures/Annotation 2020-04-06 150008.png
"  div align=center />


```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> ans = new ArrayList<>();
        if(nums.length==0){
            return ans;
        }
        quickSort(nums, 0, nums.length-1);//不需要自己，用Array.sort()
        int i = 0, k = nums.length - 1;
        int j;
        while(i<nums.length-2){
            if(nums[i]>0){
                return ans;
            }
            j=i+1;
            k=nums.length - 1;
            while(k>j){
                if(nums[i]+nums[j]+nums[k]==0){
                    List<Integer> list = new ArrayList<>();
                    list.add(nums[i]);
                    list.add(nums[j]);
                    list.add(nums[k]);
                    ans.add(list);
                    j++;
                    k--;
                    while(j<k&&nums[j]==nums[j-1]) j++;
                    while(j<k&&nums[k]==nums[k+1])  k--;

                }else if(nums[i]+nums[j]+nums[k]>0){
                    k--;
                    while(j<k&&nums[k]==nums[k+1])  k--;
                }else {
                    j++;
                    while(j<k&&nums[j]==nums[j-1]) j++;
                }
            }
            i++;
            while(i<nums.length-2&&nums[i]==nums[i-1]) i++;//跳过重复的
        }
        return ans;
    }

    public void quickSort(int[] nums, int start,int end){
        int i=start,j=end;
        int temp = nums[start];
        while(i<j){
            while(i<j&&nums[j]>temp) j--;
            if(i<j){
                nums[i] = nums[j];
                i++;
            }
            while(i<j&&nums[i]<=temp) i++;
            if(i<j){
                nums[j] = nums[i];
                j--;
            }
        }
        nums[i] = temp;
        if(start<i-1){
            quickSort(nums,start,i-1);
        }
        if(i+1<end){
            quickSort(nums,i+1,end);
        }

    }
}
```

想到了排列加双指针遍历，外层循环条件想错了。。